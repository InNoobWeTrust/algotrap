[
    {
        "name": "get_indicator_summary",
        "description": "Get a summary of technical indicator values for a specific timeframe. Returns the last 3 candles of key indicators: RSSI, ATR reversion %, structure power, Sharpe ratio, EMA200, leverage, gap zones.",
        "parameters": {
            "type": "object",
            "properties": {
                "timeframe": {
                    "type": "string",
                    "description": "The timeframe to get indicators for (e.g., '1m', '5m', '15m', '1h', '4h', '1d', '1w', '1M')"
                }
            },
            "required": [
                "timeframe"
            ]
        }
    },
    {
        "name": "get_price_action",
        "description": "Get OHLCV price action data for a specific timeframe. Returns the last N candles with open, high, low, close, volume.",
        "parameters": {
            "type": "object",
            "properties": {
                "timeframe": {
                    "type": "string",
                    "description": "The timeframe (e.g., '1h', '4h', '1d')"
                },
                "num_candles": {
                    "type": "integer",
                    "description": "Number of recent candles to return (max 20)",
                    "default": 5
                }
            },
            "required": [
                "timeframe"
            ]
        }
    },
    {
        "name": "capture_chart",
        "description": "Capture a screenshot of the multi-timeframe chart. Returns a chart image that you can analyze visually. Call this when you need to see the chart patterns, support/resistance levels, or visual confirmation of indicators.",
        "parameters": {
            "type": "object",
            "properties": {
                "timeframe": {
                    "type": "string",
                    "description": "Which timeframe to focus the chart on (e.g., '4h'). The chart will show this timeframe by default."
                }
            },
            "required": [
                "timeframe"
            ]
        }
    },
    {
        "name": "get_multi_tf_overview",
        "description": "Get a quick overview across ALL configured timeframes. Returns the latest RSSI, ATR reversion %, structure power, Sharpe, and gap zones for each timeframe. Useful for getting a bird's eye view of the market.",
        "parameters": {
            "type": "object",
            "properties": {},
            "required": []
        }
    },
    {
        "name": "read_kb",
        "description": "Read a knowledge base topic. The KB stores persistent insights across scan cycles. Valid topics: market-regimes, indicator-quirks, ticker-personalities, false-signal-patterns, successful-setups, weight-tuning-log, risk-conditions, cross-ticker-signals, timeframe-biases, lessons-learned.",
        "parameters": {
            "type": "object",
            "properties": {
                "topic": {
                    "type": "string",
                    "description": "The KB topic slug to read (e.g., 'market-regimes', 'lessons-learned')"
                }
            },
            "required": ["topic"]
        }
    },
    {
        "name": "write_kb",
        "description": "Write or append content to a knowledge base topic. Use markdown format. Content is appended to existing content. Max 2000 chars per write. Valid topics: market-regimes, indicator-quirks, ticker-personalities, false-signal-patterns, successful-setups, weight-tuning-log, risk-conditions, cross-ticker-signals, timeframe-biases, lessons-learned.",
        "parameters": {
            "type": "object",
            "properties": {
                "topic": {
                    "type": "string",
                    "description": "The KB topic slug to write to (e.g., 'lessons-learned')"
                },
                "content": {
                    "type": "string",
                    "description": "The markdown content to append to the topic file (max 2000 chars)"
                }
            },
            "required": ["topic", "content"]
        }
    },
    {
        "name": "write_notes",
        "description": "Save analysis notes to your in-session scratchpad. Use this to record key observations, conflicts, or intermediate conclusions as you analyze. Notes persist across context resets within this session but are discarded at scan end. Overwrites existing content for the same key.",
        "parameters": {
            "type": "object",
            "properties": {
                "key": {
                    "type": "string",
                    "description": "A short label for this note (e.g., 'observations', 'conflicts', 'handoff')"
                },
                "content": {
                    "type": "string",
                    "description": "The note content to save"
                }
            },
            "required": ["key", "content"]
        }
    },
    {
        "name": "read_notes",
        "description": "Read your analysis notes from the in-session scratchpad. Call with a specific key to read one note, or omit the key to read all notes.",
        "parameters": {
            "type": "object",
            "properties": {
                "key": {
                    "type": "string",
                    "description": "Optional: specific note key to read. Omit to read all notes."
                }
            },
            "required": []
        }
    }
]