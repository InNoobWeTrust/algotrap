Always code as if the guy who ends up maintaining your code will be a
violent psychopath who knows where you live. Code for readability.

Always check `~/.agents` for home-level agent config (skills, workflows,
etc.) and merge with any project-level `.agents` config. Home config
serves as the user's global defaults across all projects.
